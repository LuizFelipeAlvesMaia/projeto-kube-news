pipline{
    agent any


    stages{
        stages('Build Docker Image'){
            steps{
                script{
                    dockerapp = docker.build("luiz177/projeto-kube-news:${env.BUILD_ID}", '-f ./src/Dockerfile ./src')
                }
            }
        }
    }
}